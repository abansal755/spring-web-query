name: Generate JaCoCo Report

on:
    workflow_call:
        secrets:
            AWS_ACCESS_KEY_ID:
                required: true
            AWS_SECRET_ACCESS_KEY:
                required: true

jobs:
    jacoco:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd #v6.0.2

            - name: Set up JDK 17
              uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 #v5.2.0
              with:
                  distribution: temurin
                  java-version: "17"
                  cache: maven

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@d979d5b3a71173a29b74b5b88418bfda9437d885 #v6.1.1
              with:
                aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                aws-region: ${{ vars.AWS_REGION }}

            - name: Run tests for core package
              working-directory: ${{ github.workspace }}/spring-web-query-core
              run: mvn -B -ntp clean test -Pjacoco,boot4

            - name: Upload core jacoco report
              working-directory: ${{ github.workspace }}/spring-web-query-core/target/site
              run: |
                  aws s3 cp \
                  jacoco \
                  s3://spring-web-query-build-artifacts-444353257799-ap-south-2-an/runs/${{ github.run_id }}/spring-web-query-core-jacoco-report \
                  --recursive

                  echo "JaCoCo report for core package is available at: https://builds.akshitbansal.co.in/runs/${{ github.run_id }}/spring-web-query-core-jacoco-report/index.html"

            - name: Run tests for starter package
              working-directory: ${{ github.workspace }}/spring-boot-starter-web-query
              run: mvn -B -ntp clean test -Pjacoco,boot4

            - name: Upload starter jacoco report
              working-directory: ${{ github.workspace }}/spring-boot-starter-web-query/target/site
              run: |
                  aws s3 cp \
                  jacoco \
                  s3://spring-web-query-build-artifacts-444353257799-ap-south-2-an/runs/${{ github.run_id }}/spring-boot-starter-web-query-jacoco-report \
                  --recursive

                  echo "JaCoCo report for starter package is available at: https://builds.akshitbansal.co.in/runs/${{ github.run_id }}/spring-boot-starter-web-query-jacoco-report/index.html"

            - name: Generate workflow summary
              run: |
                  {
                    echo "## JaCoCo Reports"
                    echo ""
                    echo "| Module | Report |"
                    echo "|---|---|"
                    echo "| spring-web-query-core | [Open Report](https://builds.akshitbansal.co.in/runs/${{ github.run_id }}/spring-web-query-core-jacoco-report/index.html) |"
                    echo "| spring-boot-starter-web-query | [Open Report](https://builds.akshitbansal.co.in/runs/${{ github.run_id }}/spring-boot-starter-web-query-jacoco-report/index.html) |"
                  } >> "$GITHUB_STEP_SUMMARY"